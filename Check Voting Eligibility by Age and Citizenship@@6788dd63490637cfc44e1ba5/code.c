#include<stdio.h>
int main(){
    int age, cs;
    scanf("%d %d", &age, &cs);
    if( a >= 18 && b == 1){
        printf("Eligible");
    }else{
        printf("Not Eligible");
    }

    return 0;
}